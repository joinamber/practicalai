// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Enables strict mode for React, which helps catch issues early
  swcMinify: true, // Enables Next.js's faster SWC minifier for better performance
}

module.exports = nextConfig;
